(do
    (let outer (Func [x]
        (let inner (Func [y]
            (let z 1.0
                 r (/ 1.0 y)
            )
            (+ z r)
        ))

        (+ x (inner 2.0))
    ))

    (outer 3.0)
)
