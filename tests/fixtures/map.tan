(do
    (let user {
        "given_name" "George"
        "family_name" "Moschovitis"
        "score" 100
    })

    (let name (user "given_name"))

    name
)

; (let name (((users "gmosx") "properties") 2))
