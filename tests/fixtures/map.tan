(do
    (let map {
        "given_name" "George"
        "family_name" "Moschovitis"
        "score" 100
    })

    (let name (map "given_name"))

    name
)

; (let name (((users "gmosx") "properties") 2))
