// #TODO combine a vec of expressions into one `do` expression?, in this pass?

use crate::{ann::Ann, expr::Expr};

// #Insight
// The optimizer does not err.

pub fn optimize(_expr: Ann<Expr>) -> Ann<Expr> {
    todo!()
}
